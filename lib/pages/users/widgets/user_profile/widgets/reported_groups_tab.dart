import 'package:flutter/material.dart';

class ReportedGroupsTab extends StatelessWidget {
  const ReportedGroupsTab({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
