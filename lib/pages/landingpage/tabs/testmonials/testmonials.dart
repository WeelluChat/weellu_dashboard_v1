import 'package:flutter/widgets.dart';

class TestmonialsTab extends StatelessWidget {
  const TestmonialsTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
